---
const { eyebrow, title, body, align = 'left' } = Astro.props;
---
<div class:list={['section-heading', align === 'center' && 'center']} data-reveal>
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  {body && <p class="section-intro">{body}</p>}
</div>
